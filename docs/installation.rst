============
Installation
============

At the command line::

    $ easy_install graphDbWithAdapter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv graphDbWithAdapter
    $ pip install graphDbWithAdapter
